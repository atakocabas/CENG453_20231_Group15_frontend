<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.paint.Color?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.shape.Circle?>
<?import javafx.scene.text.Text?>

<?import javafx.scene.layout.HBox?>


<?import javafx.geometry.Insets?>
<VBox xmlns="http://javafx.com/javafx/8.0.171" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.catan.catanui.controller.game.PlayerController" spacing="10" alignment="TOP_CENTER">
    <padding>
        <Insets top="20" right="20" bottom="20" left="10" />
    </padding>

    <HBox spacing="10" alignment="CENTER">
        <VBox spacing="5">
            <HBox>
                <Text text="Hills: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player1Hills" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Forest: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player1Forest" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Mountains: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player1Mountains" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Fields: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player1Fields" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Pasture: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player1Pasture" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
        </VBox>
        <VBox spacing="5">
            <padding>
                <Insets top="20" />
            </padding>
            <Circle fx:id="player1Circle" radius="25" fill="greenyellow" stroke="black"/>
            <Text text="  Player 1" style="-fx-font-size: ${PlayerController.fontSize}; -fx-fill: ${PlayerController.textColor};" />
        </VBox>
    </HBox>

    <HBox spacing="10" alignment="CENTER">
        <padding>
            <Insets top="20" />
        </padding>
        <VBox spacing="5">
            <HBox>
                <Text text="Hills: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player2Hills" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Forest: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player2Forest" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Mountains: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player2Mountains" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Fields: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player2Fields" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Pasture: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player2Pasture" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
        </VBox>
        <VBox spacing="5">
            <padding>
                <Insets top="20" />
            </padding>
            <Circle fx:id="player2Circle" radius="25" fill="orangered" stroke="black"/>
            <Text text="  Player 2" style="-fx-font-size: ${PlayerController.fontSize}; -fx-fill: ${PlayerController.textColor};" />
        </VBox>
    </HBox>

    <HBox spacing="10" alignment="CENTER">
        <padding>
            <Insets top="20" />
        </padding>
        <VBox spacing="5">
            <HBox>
                <Text text="Hills: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player3Hills" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Forest: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player3Forest" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Mountains: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player3Mountains" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Fields: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player3Fields" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Pasture: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player3Pasture" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
        </VBox>
        <VBox spacing="5">
            <padding>
                <Insets top="20" />
            </padding>
            <Circle fx:id="player3Circle" radius="25" fill="orange" stroke="black"/>
            <Text text="  Player 3" style="-fx-font-size: ${PlayerController.fontSize}; -fx-fill: ${PlayerController.textColor};" />
        </VBox>
    </HBox>

    <HBox spacing="10" alignment="CENTER">
        <padding>
            <Insets top="20" />
        </padding>
        <VBox spacing="5">
            <HBox>
                <Text text="Hills: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player4Hills" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Forest: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player4Forest" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Mountains: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player4Mountains" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Fields: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player4Fields" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
            <HBox>
                <Text text="Pasture: " style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
                <Text fx:id="player4Pasture" text="0" style="-fx-font-size: ${playerController.fontSize}px; -fx-fill: ${playerController.textColor};"/>
            </HBox>
        </VBox>
        <VBox spacing="5">
            <padding>
                <Insets top="20" />
            </padding>
            <Circle fx:id="player4Circle" radius="25" fill="purple" stroke="black"/>
            <Text text="  Player 4" style="-fx-font-size: ${PlayerController.fontSize}; -fx-fill: ${PlayerController.textColor};" />
        </VBox>
    </HBox>


</VBox>
